#1858. Longest word with all prefixes:

"""
words = ["a", "banana", "app", "appl", "ap", "apply",
"apple"]
words = ["k", "ki", "kir", "kira", "kiran"]
words = ["abc", "bc", "ab", qwe]
#product sense - busy pricing(surge pricing) vs no busy pricing
#What metrics to measure? What metrics to develop?
#Randomize by region or by time?
#expected changes in user behaviors/ expected changes in key metrics.
"""

L = ["Geeks\n", "for\n", "Geeks\n"]
file1 = open('myfile.txt', "w")
file1.writelines(L)
file1.close()

class Solution:
    def longestWord(self, words: List[str]) -> str:
