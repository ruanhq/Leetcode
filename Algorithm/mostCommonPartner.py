#Find the most common partner:

"""
[["Casper", "Purple", "Wayfair", "Wayfair"],
["Purple", "Wayfair", "Tradesy"],
["Wayfair", "Tradesy", "Peloton"]]
"""

#To identify the most common partner for each of the person:

#We first construct a data structure(json) mapping each of the word to the 
#mapping 


#To generate a nested json object here:
dic = collections.defaultdict(list)
dic[key].append(value)
for key in dic:
    print key, ":", dic[x], "\n"


